spring:
  flyway:
    enabled: true
    baseline-on-migrate: true
    validate-on-migrate: false
    locations:
      - classpath:db/migration
      - classpath:db/seed

logging:
  level:
    org.flywaydb: DEBUG

---
spring:
  config:
    activate:
      on-profile: dev

  flyway:
    clean-disabled: true
    validate-on-migrate: true

---
spring:
  config:
    activate:
      on-profile: prod

  flyway:
    clean-disabled: true
    validate-on-migrate: true
    locations:
      - classpath:db/migration
